(define (max int1 int2) ; returns the max of given two integers
  (if (> int1 int2) int1 int2)) ;; helper func
  
;;


; (define sort)


; void MR::do_sort(vector<Pair> &vec) {
;   sort(vec.begin(), vec.end(), compare);
; }
  
